// Design assets come from the MediDent Istanbul.dc.html package `assets/` folder.
// WordPress scraping is intentionally disabled — do not reintroduce it.
console.error(`
Design görselleri GitHub'a yüklenmemiş.

OKUBENI.txt'te geçen assets/ klasörünü (portrait-a.jpg, aug-17-*.png, logo.png, ...)
zip olarak bu sohbete veya repo köküne yükleyin.

Logolar MediDent_pdf.pdf'den alındı: src/assets/images/logo*.png
`);
process.exit(1);
